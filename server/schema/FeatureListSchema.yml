type: object
required:
  - features
properties:
  features:
    type: array
    items:
      type: object
      required:
        - name
        - vesion
      properties:
        name:
          type: string
          description: name of feature
          faker: random.word
        version:
          type: string
          description: version of feature
          faker: random.word
